for backend when using nginx

1. set the base route as /api

2. add script in package.json
    "dev": "nodemon index.js"

3. connect mongodb using 
        PORT=8000
        MONGO_URI=mongodb://mongo:27017/shareit
